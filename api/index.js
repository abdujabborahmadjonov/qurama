"use strict";
/* Serverless entry point (Vercel). vercel.json routes everything here, and
   Express handles it exactly as it does in a long-lived process. */
module.exports = require("../server/app");
