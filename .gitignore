node_modules/

# Secrets. SUPABASE_SERVICE_ROLE_KEY can read every contributor's contact
# details — it must never reach the repository.
.env
.env.*
!.env.example

# Vercel build output
.vercel/

.DS_Store
